<%= form_for(:session, url: login_post_path) do |f| %>

    <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email %>
    </div>

    <div class="field">
        <%= f.label :password, "Passphrase" %><br />
        <%= f.password_field :password %>
    </div>

    <div class="field action">
        <%= f.submit "Login" %>
        <%= link_to "Forgot Password", password_reset_path %>
    </div>

<% end %>